@page
@model PX_Project_Version_3.Pages.VoteCounts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VoteCount[0].TeamID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VoteCount[0].TeamName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VoteCount[0].EventID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VoteCount[0].TotalCounts)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VoteCount) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TeamID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeamName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalCounts)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.VoteCountId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.VoteCountId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.VoteCountId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
