@page
@model PX_Project_Version_3.Pages.TieBreakModel

@{
    ViewData["Title"] = "TieBreak";
}

<h1>TieBreak</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TieBreaker[0].TeamID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TieBreaker[0].UserID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TieBreaker[0].EventID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TieBreaker) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TeamID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TieBreakerId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TieBreakerId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TieBreakerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
